# Toolkit configuration — {{WORKSPACE_NAME}} workspace

> Generated by `swift-toolkit:workspace-init`.
> Toolkit-managed: `swift-lang`, `architecture-choice`, `task-new`, `workspace-*` write here.

## Language

en

## Mode

manual

## Stack

multi-package-spm-workspace

## Persona

Workspace persona — multi-package SPM workspace (meta-repo + N package repos + optional project-repo).

## Workspace meta

- Workspace name: {{WORKSPACE_NAME}}
- Layout: see `workspace.yml` (single source of truth).
- Schema reference: `docs/workspace-yml-reference.md`.

## Validation

(populated by `swift-toolkit:architecture-choice` per package as needed)
